# Responsible: Loy

amigo1:

    amiddle-cleanup

amigo3:

    roslaunch run_speech_files speech_tue_test_lab.launch

amigo3:

    roslaunch speech_interpreter start_tue_test_lab.launch 

amigo3:

    alsamixer
        Mic Boost -> 100
        Capture -> 44
        Digital -> 29
        Mic on mute!!! (Should be muted already)
        
    rosservice call /interpreter/get_continue "n_tries_max: 10 time_out: 100.0"

    say test

    rosrun challenge_cleanup clean_up.py

TIP of the day: use 
$ rostopic pub /speech_cleanup/output std_msgs/String 'livingroom' --once; rostopic pub /speech_yesno/output std_msgs/String '!!str yes' --once
to test the exec

---------------------------------------
BUGS: Remove bug if fixed:

- No meetingpoints defined for tue_testlab-environment. --> code DONE, test needed.
